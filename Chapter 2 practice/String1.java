public class String1
{
    public static void main(String[] args)
    {
        String greeting = "Hello, World";
        int n = greeting.length();
        
        System.out.println("length of string: " + n);
        
        String river = "Mississippi";
        String bigRiver = river.toUpperCase();
        System.out.println("river: " + river + " bigRiver: " + bigRiver);
    }
}
/* string variable can have two strings in one, also if were adding something to a string then whatever
 * that string is it will become a string. Nothing can modify the string at all, it just duplicates it
   and makes changes to the new one*/
 